---
phase: 01-vault-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-vault-integrity/01-REVIEW-LOG.md
  - lesson-modules/3-memory-partner/3.5-inbox-pattern/CLAUDE.md
autonomous: true
requirements: [VAULT-01, VAULT-02]

must_haves:
  truths:
    - "Every wikilink in teaching scripts either resolves to an existing vault note or is documented as intentional/uncertain in the review log"
    - "Every wikilink in vault notes resolves to an existing vault note or is documented in the review log"
    - "The review log contains sections for Uncertain Links, Plain Text References, Cross-Module Mismatches, and Intentional Unresolved Links"
  artifacts:
    - path: ".planning/phases/01-vault-integrity/01-REVIEW-LOG.md"
      provides: "Categorized audit of all wikilink issues"
      contains: "## Uncertain Links"
  key_links:
    - from: "01-REVIEW-LOG.md"
      to: "teaching scripts + vault notes"
      via: "wikilink cross-reference against vault note inventory"
      pattern: "INTENTIONAL|UNCERTAIN"
---

<objective>
Audit all wikilinks across 24 teaching scripts and 37 vault notes. Fix confident matches, categorize intentional unresolved links (notes created during lessons), and log uncertain cases. Produce the combined review log that the user will reference for manual decisions.

Purpose: VAULT-01 and VAULT-02 require that every wikilink either resolves or is accounted for. This audit is the foundation the rest of Phase 1 builds on.
Output: 01-REVIEW-LOG.md with four sections; any fixable wikilink corrections applied to teaching scripts.
</objective>

<execution_context>
@/Users/benest/.claude/get-shit-done/workflows/execute-plan.md
@/Users/benest/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-vault-integrity/01-CONTEXT.md
@.planning/phases/01-vault-integrity/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract and cross-reference all wikilinks against vault inventory</name>
  <files>.planning/phases/01-vault-integrity/01-REVIEW-LOG.md</files>
  <read_first>
    - .planning/phases/01-vault-integrity/01-RESEARCH.md (contains the complete verified findings — 9 missing-from-vault wikilinks in scripts, 0 unresolved vault-to-vault links, intentional vs uncertain categorization, and module ordering verification)
    - .planning/phases/01-vault-integrity/01-CONTEXT.md (locked decisions on fix strategy, review log format, intentional unresolved link handling, plain text reference flagging, cross-module note name consistency checks)
    - course-structure.json (module ordering — needed to verify that notes containing unresolved links appear at or before the module that creates the target note)
  </read_first>
  <action>
Build the vault note inventory and cross-reference all wikilinks. Follow these exact steps:

**Step 1: Build vault note inventory.**
Run: `find /Users/benest/dev/claude-brain/vault -name "*.md" -not -path "*/\.obsidian/*" | sort`
Extract just the filename without extension from each path (this is how Obsidian resolves wikilinks). Store as the canonical note list.

**Step 2: Extract all wikilinks from teaching scripts.**
Run: `grep -rn "\[\[" /Users/benest/dev/claude-brain/lesson-modules/ --include="*.md" | grep -oE "\[\[[^]]+\]\]" | sort -u`
For each wikilink, extract the target note name (the part before `|` if alias exists). Compare against vault note inventory.

**Step 3: Extract all wikilinks from vault notes.**
Run: `grep -rn "\[\[" /Users/benest/dev/claude-brain/vault/ --include="*.md" -not -path "*/\.obsidian/*" | grep -oE "\[\[[^]]+\]\]" | sort -u`
Exclude folder-style links ending in `/` (e.g., `[[00-Inbox/]]`). Compare remaining against vault note inventory.

**Step 4: Categorize unresolved teaching script wikilinks using the RESEARCH.md findings.**
The research already identified these 9 wikilinks missing from vault:

INTENTIONAL (created during lessons — document with source note and creating module):
- `[[Synthesis - AI Creativity and Intentionality]]` — created by module 1.4
- `[[GitLab Async Communication Model]]` — created by module 2.1
- `[[AI as Extended Memory]]` — created by module 2.1
- `[[AI and Creative Work]]` — created by module 2.2 (MOC)
- `[[Client Work Dashboard]]` — created by module 2.2 (MOC)
- `[[Research - Digital Sunset Routine]]` — created by module 3.5

UNCERTAIN (no module creates them — log for manual review):
- `[[The Notification Audit]]` — referenced in module 3.5; likely refers to Bloom Post 1 per CLIENTS.md but no vault note exists with that exact name
- `[[Tools for Thought - The Design Philosophy]]` — referenced 3 times in module 3.5; no module creates this note

SPECIAL CASE (document as intentional):
- `[[The Synthesis]]` — newsletter name used as conceptual reference inside note content created by module 2.1, not a vault note link

**Step 5: Verify the research findings are still accurate** by running the grep extraction and confirming the exact same results. If any new unresolved links are found that RESEARCH.md did not list, categorize them using the same rules.

**Step 6: Flag plain-text references to vault notes in teaching scripts.**
Search each teaching script for plain-text mentions of vault note names (without `[[ ]]` brackets). These are strings that match a vault note filename but appear as plain text. Log any found in the "Plain Text References" section. Common patterns to check: backtick-quoted file paths (these are fine — they are CLI references, not wikilinks) vs prose mentions of note titles without any formatting.

**Step 7: Cross-module note name consistency check.**
Read course-structure.json to get the module ordering. For each module that instructs students to create a note, check that the note name matches what later modules reference. Log any mismatches in "Cross-Module Mismatches" section. This is an identification-only step — do not fix mismatches (that is Phase 2 work per CONTEXT.md).

**Step 8: Create `.planning/phases/01-vault-integrity/01-REVIEW-LOG.md`** with this structure:
```markdown
# Phase 1: Vault Integrity - Review Log

**Generated:** [date]
**Purpose:** Manual review items identified during wikilink audit

## Intentional Unresolved Links

[Table with columns: Wikilink Target | Source Note/Script | Creating Module | Module Order Valid?]
Include all 6 intentional + 1 special case (The Synthesis).

## Uncertain Links

[Table with columns: Wikilink Target | Source Note/Script | Context | Recommendation]
Include The Notification Audit and Tools for Thought - The Design Philosophy.

## Plain Text References

[Table with columns: Text Reference | Source Script | Line | Should Be Wikilink?]
List any plain-text vault note name mentions found in Step 6.

## Cross-Module Mismatches

[Table with columns: Note Name Created (Module) | Note Name Expected (Module) | Discrepancy]
List any mismatches found in Step 7. If none found, state "No cross-module name mismatches detected."

## Vault-to-Vault Wikilinks

Status: All 39 vault-to-vault wikilinks resolve. Zero unresolved. Folder-style links (`[[00-Inbox/]]`, `[[06-Templates/]]`) excluded as illustrative prose, not note links.

## Summary

- Teaching script wikilinks: X total unique, Y unresolved (Z intentional, W uncertain)
- Vault-to-vault wikilinks: All resolve
- Plain text references: N flagged
- Cross-module mismatches: M found
```
  </action>
  <verify>
    <automated>grep -c "## Intentional Unresolved Links" /Users/benest/dev/claude-brain/.planning/phases/01-vault-integrity/01-REVIEW-LOG.md && grep -c "## Uncertain Links" /Users/benest/dev/claude-brain/.planning/phases/01-vault-integrity/01-REVIEW-LOG.md && grep -c "## Plain Text References" /Users/benest/dev/claude-brain/.planning/phases/01-vault-integrity/01-REVIEW-LOG.md && grep -c "## Cross-Module Mismatches" /Users/benest/dev/claude-brain/.planning/phases/01-vault-integrity/01-REVIEW-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - 01-REVIEW-LOG.md exists at `.planning/phases/01-vault-integrity/01-REVIEW-LOG.md`
    - File contains `## Intentional Unresolved Links` section with at least 6 entries (the 6 intentional + The Synthesis special case)
    - File contains `## Uncertain Links` section with entries for `The Notification Audit` and `Tools for Thought - The Design Philosophy`
    - File contains `## Plain Text References` section (may be empty if none found)
    - File contains `## Cross-Module Mismatches` section
    - File contains `## Vault-to-Vault Wikilinks` section stating all resolve
    - File contains `## Summary` section with counts
  </acceptance_criteria>
  <done>Review log exists with all four required sections populated. Every wikilink across teaching scripts and vault notes is accounted for — either resolving to a vault note, categorized as intentional, or logged as uncertain for manual review.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the two uncertain wikilinks in module 3.5 teaching script</name>
  <files>lesson-modules/3-memory-partner/3.5-inbox-pattern/CLAUDE.md</files>
  <read_first>
    - lesson-modules/3-memory-partner/3.5-inbox-pattern/CLAUDE.md (the file being modified — must read current state to understand context of each wikilink usage)
    - .planning/phases/01-vault-integrity/01-REVIEW-LOG.md (the review log created in Task 1, confirming which links are uncertain)
    - .planning/phases/01-vault-integrity/01-CONTEXT.md (locked decision: "When a wikilink points to a non-existent note, rename it to the closest matching vault note IF the match is confident. When no confident match exists, log in review log for manual review.")
    - creator-context/CLIENTS.md (needed to verify "The Notification Audit" is Bloom Post 1 title and determine if a confident rename is possible)
  </read_first>
  <action>
Read module 3.5's teaching script and find the exact lines containing `[[The Notification Audit]]` and `[[Tools for Thought - The Design Philosophy]]`.

**For `[[The Notification Audit]]`:**
- This appears in the Librarian agent's generated output within module 3.5
- Per CLIENTS.md, "The Notification Audit" is the title of Bloom Studio Post 1
- There is no vault note with this exact name. The closest vault note is `Digital Boundaries Series - Overview.md` which covers the series, but that is not a match for a single post title
- Per the locked decision: no confident match exists, so this stays in the review log as uncertain. Do NOT rename it.
- If the wikilink appears inside content that Claude writes to the vault during the lesson (inside a string/template that gets written as a note), it may be intentional as a forward reference. Assess context.

**For `[[Tools for Thought - The Design Philosophy]]`:**
- Referenced 3 times in module 3.5
- No module creates this note. There IS a vault note `Tools for Thought History.md` in `03-Research/`
- Assess if `[[Tools for Thought - The Design Philosophy]]` is a confident rename to `[[Tools for Thought History]]` — compare the context in which it appears. If the script references it as a research note about tools-for-thought history/philosophy, renaming to `[[Tools for Thought History]]` is a confident match
- If renaming: preserve any display aliases. Change `[[Tools for Thought - The Design Philosophy]]` to `[[Tools for Thought History]]` (or `[[Tools for Thought History|Tools for Thought - The Design Philosophy]]` if the original text serves as a better display name in context)
- If NOT a confident match: leave unchanged and ensure it remains in the review log

**After assessment, apply fixes to `lesson-modules/3-memory-partner/3.5-inbox-pattern/CLAUDE.md`** using the Edit tool for each change. Update the review log's Uncertain Links section to reflect any links that were resolved vs those that remain uncertain.
  </action>
  <verify>
    <automated>grep -n "Tools for Thought" /Users/benest/dev/claude-brain/lesson-modules/3-memory-partner/3.5-inbox-pattern/CLAUDE.md | head -10</automated>
  </verify>
  <acceptance_criteria>
    - Module 3.5 CLAUDE.md has been read and assessed for both uncertain wikilinks
    - If `[[Tools for Thought - The Design Philosophy]]` was a confident match to `[[Tools for Thought History]]`: all 3 occurrences are renamed (grep for `Tools for Thought History` returns 3 matches in the file)
    - If not a confident match: the links remain unchanged and 01-REVIEW-LOG.md still lists them as uncertain
    - `[[The Notification Audit]]` handling is documented — either fixed (if confident match found) or confirmed as uncertain in review log
    - No wikilink display aliases were removed during any renaming
    - 01-REVIEW-LOG.md is updated to reflect current state of uncertain links (resolved count vs remaining)
  </acceptance_criteria>
  <done>All uncertain wikilinks in teaching scripts are either confidently renamed to match existing vault notes or explicitly documented as requiring manual review. The review log reflects the final state.</done>
</task>

</tasks>

<verification>
- Run `grep -rn "\[\[" /Users/benest/dev/claude-brain/lesson-modules/ --include="*.md" | grep -oE "\[\[[^]]+\]\]" | sort -u` and confirm every extracted wikilink target either exists in vault or is documented in 01-REVIEW-LOG.md
- Run `grep -rn "\[\[" /Users/benest/dev/claude-brain/vault/ --include="*.md" -not -path "*/.obsidian/*" | grep -oE "\[\[[^]]+\]\]" | sort -u` and confirm every target (excluding folder-style links) resolves to a vault note
- Confirm 01-REVIEW-LOG.md has all four required sections with content
</verification>

<success_criteria>
- Every teaching script wikilink is either resolved, intentionally unresolved (documented), or logged as uncertain
- Every vault-to-vault wikilink resolves (already true per research — verify it remains true)
- 01-REVIEW-LOG.md exists with Intentional, Uncertain, Plain Text References, and Cross-Module Mismatches sections
- No wikilink aliases were broken during any fixes
</success_criteria>

<output>
After completion, create `.planning/phases/01-vault-integrity/01-01-SUMMARY.md`
</output>
