---
phase: 01-vault-integrity
plan: 04
type: execute
wave: 1
depends_on: [01-01]
files_modified:
  - lesson-modules/3-memory-partner/3.5-inbox-pattern/CLAUDE.md
  - .planning/phases/01-vault-integrity/01-REVIEW-LOG.md
autonomous: true
gap_closure: true
requirements: [VAULT-01]

must_haves:
  truths:
    - "Every [[wikilink]] in teaching scripts either resolves to an existing vault note or is corrected to match reality"
    - "Module 3.5 does not deposit a broken wikilink into the student's vault when it runs"
  artifacts:
    - path: "lesson-modules/3-memory-partner/3.5-inbox-pattern/CLAUDE.md"
      provides: "Corrected line 304 — no broken wikilink in vault content block"
      contains: "Build on the foundation from The Notification Audit (Post 1)"
    - path: ".planning/phases/01-vault-integrity/01-REVIEW-LOG.md"
      provides: "Updated Uncertain Links section showing resolution"
      contains: "RESOLVED"
  key_links:
    - from: "lesson-modules/3-memory-partner/3.5-inbox-pattern/CLAUDE.md"
      to: "vault content written during module 3.5"
      via: "markdown block at line ~304 written to Research - Digital Sunset Routine.md"
      pattern: "Build on the foundation from The Notification Audit"
---

<objective>
Close the single verification gap blocking Phase 1 completion: convert the unresolved `[[The Notification Audit]]` wikilink at line 304 of module 3.5's teaching script from a wikilink to plain text, so students completing module 3.5 do not end up with a broken link in their vault.

Purpose: VAULT-01 requires all teaching script wikilinks to either resolve or be corrected. The verification report scored 4/5 success criteria — this is the one remaining gap.
Output: Corrected teaching script line, updated review log documenting the resolution.
</objective>

<execution_context>
@/Users/benest/.claude/get-shit-done/workflows/execute-plan.md
@/Users/benest/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-vault-integrity/01-01-SUMMARY.md
@.planning/phases/01-vault-integrity/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert [[The Notification Audit]] to plain text in module 3.5</name>
  <files>lesson-modules/3-memory-partner/3.5-inbox-pattern/CLAUDE.md, .planning/phases/01-vault-integrity/01-REVIEW-LOG.md</files>
  <read_first>
    - lesson-modules/3-memory-partner/3.5-inbox-pattern/CLAUDE.md (lines 295-315 — the content block containing the broken wikilink at line 304)
    - .planning/phases/01-vault-integrity/01-REVIEW-LOG.md (lines 24-32 — the Uncertain Links section documenting this issue)
    - .planning/phases/01-vault-integrity/01-VERIFICATION.md (lines 1-20 — the gap definition and resolution options)
    - creator-context/CLIENTS.md (confirm "The Notification Audit" is Bloom Post 1 title — this validates that plain text is semantically correct)
  </read_first>
  <action>
Two edits in two files:

**Edit 1 — Fix the broken wikilink (module 3.5 teaching script):**

In `lesson-modules/3-memory-partner/3.5-inbox-pattern/CLAUDE.md`, line 304, change:

```
Build on the foundation from [[The Notification Audit]] (Post 1) — "Now that you've audited your notifications, here's what to do with that awareness in the evening."
```

to:

```
Build on the foundation from The Notification Audit (Post 1) — "Now that you've audited your notifications, here's what to do with that awareness in the evening."
```

This removes the `[[` and `]]` wikilink brackets around "The Notification Audit", converting it to plain text. The parenthetical "(Post 1)" already provides context. No vault note named "The Notification Audit" exists, and no module creates one, so this link would always be broken for students.

Why plain text over the other options:
- Option A (create stub note) adds content to the vault, which is out of Phase 1 scope (no new content except .gitkeep)
- Option B (alias to Overview note) would be misleading — the Overview covers all 4 posts, not just Post 1
- Option C (plain text) preserves meaning with zero content loss and no broken link in the student's vault

**Edit 2 — Update the review log:**

In `.planning/phases/01-vault-integrity/01-REVIEW-LOG.md`, update the `[[The Notification Audit]]` row in the Uncertain Links table (line 30). Change the Recommendation column from:

```
**Review required:** Decide whether to (A) create a vault stub note `The Notification Audit.md` as a project note for Bloom Post 1, or (B) change the wikilink to `[[Digital Boundaries Series - Overview\|The Notification Audit]]`, or (C) leave as plain text since no vault note exists yet.
```

to:

```
**RESOLVED (gap closure plan 01-04):** Converted to plain text — removed `[[` `]]` brackets. No vault note with this name exists and no module creates one. Plain text preserves the Post 1 title reference without depositing a broken wikilink into the student's vault. Options A (stub note) and B (alias to Overview) were rejected: A adds content beyond Phase 1 scope, B is misleading since the Overview covers all 4 posts.
```
  </action>
  <verify>
    <automated>grep -n "The Notification Audit" lesson-modules/3-memory-partner/3.5-inbox-pattern/CLAUDE.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "\[\[The Notification Audit\]\]" lesson-modules/3-memory-partner/3.5-inbox-pattern/CLAUDE.md` returns 0 matches (no remaining wikilink brackets)
    - `grep "Build on the foundation from The Notification Audit (Post 1)" lesson-modules/3-memory-partner/3.5-inbox-pattern/CLAUDE.md` returns exactly 1 match (plain text version present)
    - `grep "RESOLVED" .planning/phases/01-vault-integrity/01-REVIEW-LOG.md` returns at least 1 match (review log updated)
    - `grep -c "\[\[" lesson-modules/3-memory-partner/3.5-inbox-pattern/CLAUDE.md` count is exactly 1 fewer than before the edit (was N, now N-1)
    - No other lines in the file were modified (diff shows exactly 1 line changed in the teaching script)
  </acceptance_criteria>
  <done>Line 304 of module 3.5 teaching script contains plain text "The Notification Audit" instead of wikilink brackets. Review log Uncertain Links section shows the resolution with reasoning. Zero unresolved uncertain wikilinks remain in the review log.</done>
</task>

</tasks>

<verification>
After task completion, run these commands to verify the gap is fully closed:

1. Confirm no `[[The Notification Audit]]` wikilink remains anywhere in teaching scripts:
   ```bash
   grep -rn "\[\[The Notification Audit\]\]" lesson-modules/ --include="*.md"
   ```
   Expected: 0 results

2. Confirm the plain text reference is present:
   ```bash
   grep -n "Build on the foundation from The Notification Audit" lesson-modules/3-memory-partner/3.5-inbox-pattern/CLAUDE.md
   ```
   Expected: 1 result at line ~304

3. Confirm review log documents the resolution:
   ```bash
   grep "RESOLVED" .planning/phases/01-vault-integrity/01-REVIEW-LOG.md
   ```
   Expected: 1+ results

4. Re-verify VAULT-01 completeness — all teaching script wikilinks now either resolve or are corrected:
   ```bash
   grep -rn "\[\[" lesson-modules/ --include="*.md" | grep -oE "\[\[[^]]+\]\]" | sort -u
   ```
   Cross-reference output against vault note inventory. All targets should either exist in vault/ or be documented as intentional in the review log.
</verification>

<success_criteria>
- VAULT-01 fully satisfied: every [[wikilink]] in teaching scripts either resolves to an existing vault note, is intentionally unresolved (documented), or is corrected to match reality
- Module 3.5 no longer deposits a broken wikilink into the student's vault
- Review log reflects the resolution with clear reasoning
- Phase 1 verification score moves from 4/5 to 5/5
</success_criteria>

<output>
After completion, create `.planning/phases/01-vault-integrity/01-04-SUMMARY.md`
</output>
