---
phase: 07-renumbering-text-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lesson-modules/4-automation/4.4-mcp-tools/CLAUDE.md
  - lesson-modules/4-automation/4.3-librarian-agent/CLAUDE.md
  - lesson-modules/4-automation/4.5-skills-integration/CLAUDE.md
  - .claude/commands/start-4-6.md
autonomous: true
requirements:
  - CONT-04
  - SKIL-01

must_haves:
  truths:
    - "Module 4.4 closing Say block tells the student the next module covers Claude Code Skills, not custom slash commands"
    - "Module 4.3 forward-reference parenthetical says (4.6) for custom slash commands, not (4.5)"
    - "Module 4.5 instructor note correctly states the student has NOT yet covered custom slash commands"
    - "start-4-6.md command description says Explore and customize, not Build"
  artifacts:
    - path: "lesson-modules/4-automation/4.4-mcp-tools/CLAUDE.md"
      provides: "Accurate closing Say block describing next module as Claude Code Skills"
      contains: "we're exploring Claude Code Skills"
    - path: "lesson-modules/4-automation/4.3-librarian-agent/CLAUDE.md"
      provides: "Correct forward-reference module number for custom slash commands"
      contains: "In the next module (4.6)"
    - path: "lesson-modules/4-automation/4.5-skills-integration/CLAUDE.md"
      provides: "Accurate instructor note about student progress"
      contains: "has not yet covered custom slash commands"
    - path: ".claude/commands/start-4-6.md"
      provides: "Accurate command description matching explore-and-customize teaching philosophy"
      contains: "Explore and customize your vault"
  key_links:
    - from: "lesson-modules/4-automation/4.4-mcp-tools/CLAUDE.md"
      to: "lesson-modules/4-automation/4.5-skills-integration/CLAUDE.md"
      via: "closing Say block describes what /start-4-5 leads to"
      pattern: "exploring Claude Code Skills.*start-4-5"
    - from: "lesson-modules/4-automation/4.3-librarian-agent/CLAUDE.md"
      to: "lesson-modules/4-automation/4.6-custom-slash-commands/CLAUDE.md"
      via: "forward reference parenthetical (4.6)"
      pattern: "next module \\(4\\.6\\)"
---

<objective>
Fix four stale module number/description references in Level 4 teaching scripts left behind by Phase 4's renumbering (insertion of Module 4.5 Claude Code Skills).

Purpose: Students currently see mismatched descriptions at module transitions -- Module 4.4 promises "custom slash commands" but the next module is actually "Claude Code Skills." These four text fixes eliminate every known stale reference so the student experience is seamless.

Output: Four corrected text passages across three teaching scripts and one command file.
</objective>

<execution_context>
@/Users/benest/.claude/get-shit-done/workflows/execute-plan.md
@/Users/benest/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-renumbering-text-fixes/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Module 4.4 closing description and Module 4.3 forward-reference number</name>
  <files>lesson-modules/4-automation/4.4-mcp-tools/CLAUDE.md, lesson-modules/4-automation/4.3-librarian-agent/CLAUDE.md</files>
  <read_first>
    - lesson-modules/4-automation/4.4-mcp-tools/CLAUDE.md (lines 250-260 — the closing Say block with stale description)
    - lesson-modules/4-automation/4.3-librarian-agent/CLAUDE.md (lines 220-230 — the forward-reference paragraph)
  </read_first>
  <action>
Two edits in two files:

**Edit 1 — Module 4.4 closing Say block (line 255):**

Replace the exact line:
```
Next up, we're building custom slash commands — turning your most common workflows into one-word actions. Type `/start-4-5` to continue."
```

With:
```
Next up, we're exploring Claude Code Skills — a new way to teach Claude new abilities. Type `/start-4-5` to continue."
```

IMPORTANT: The `/start-4-5` command reference must remain unchanged. Only the topic description changes.

**Edit 2 — Module 4.3 forward reference (line 223):**

Replace the exact text:
```
In the next module (4.5), we'll actually build a custom slash command that runs the Librarian scan
```

With:
```
In the next module (4.6), we'll actually build a custom slash command that runs the Librarian scan
```

IMPORTANT: Only the parenthetical number changes from "(4.5)" to "(4.6)". The rest of the sentence stays identical. The navigation command `/start-4-4` on line 245 must NOT be changed.
  </action>
  <verify>
    <automated>grep -n "exploring Claude Code Skills" lesson-modules/4-automation/4.4-mcp-tools/CLAUDE.md && grep -n "next module (4.6)" lesson-modules/4-automation/4.3-librarian-agent/CLAUDE.md && grep -c "next module (4.5)" lesson-modules/4-automation/4.3-librarian-agent/CLAUDE.md | grep "^0$" && grep -c "building custom slash commands" lesson-modules/4-automation/4.4-mcp-tools/CLAUDE.md | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - lesson-modules/4-automation/4.4-mcp-tools/CLAUDE.md line 255 contains "we're exploring Claude Code Skills — a new way to teach Claude new abilities"
    - lesson-modules/4-automation/4.4-mcp-tools/CLAUDE.md line 255 still contains "/start-4-5" (navigation unchanged)
    - lesson-modules/4-automation/4.4-mcp-tools/CLAUDE.md contains zero occurrences of "building custom slash commands"
    - lesson-modules/4-automation/4.3-librarian-agent/CLAUDE.md line 223 contains "next module (4.6)"
    - lesson-modules/4-automation/4.3-librarian-agent/CLAUDE.md contains zero occurrences of "next module (4.5)"
    - lesson-modules/4-automation/4.3-librarian-agent/CLAUDE.md still contains "/start-4-4" navigation on line 245 (untouched)
  </acceptance_criteria>
  <done>Module 4.4 closing Say accurately describes next module as Claude Code Skills. Module 4.3 forward reference correctly points to Module 4.6 for custom slash commands.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Module 4.5 instructor note and start-4-6.md command description</name>
  <files>lesson-modules/4-automation/4.5-skills-integration/CLAUDE.md, .claude/commands/start-4-6.md</files>
  <read_first>
    - lesson-modules/4-automation/4.5-skills-integration/CLAUDE.md (lines 1-15 — the instructor "Your Role" section with stale completion claim)
    - .claude/commands/start-4-6.md (full file — only 10 lines, contains stale description frontmatter)
  </read_first>
  <action>
Two edits in two files:

**Edit 1 — Module 4.5 instructor note (line 11):**

Find the exact text:
```
The student has already learned about slash commands (Module 4.6) and sub-agents (Module 3.2). Skills complete the trio.
```

Replace with:
```
The student has not yet covered custom slash commands (coming in Module 4.6) and has already met sub-agents (Module 3.2). Skills complete the trio.
```

IMPORTANT: Only the first sentence changes. "Skills complete the trio." remains at the end. The three-part framing in the rest of the paragraph must not be altered.

**Edit 2 — start-4-6.md command description (line 2):**

Find the exact text:
```
description: "Module 4.6: Custom Slash Commands - Build /daily, /weekly-review, /process-inbox"
```

Replace with:
```
description: "Module 4.6: Custom Slash Commands - Explore and customize your vault's commands"
```
  </action>
  <verify>
    <automated>grep -n "has not yet covered custom slash commands" lesson-modules/4-automation/4.5-skills-integration/CLAUDE.md && grep -n "Explore and customize" .claude/commands/start-4-6.md && grep -c "has already learned about slash commands" lesson-modules/4-automation/4.5-skills-integration/CLAUDE.md | grep "^0$" && grep -c "Build /daily" .claude/commands/start-4-6.md | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - lesson-modules/4-automation/4.5-skills-integration/CLAUDE.md line 11 contains "has not yet covered custom slash commands (coming in Module 4.6)"
    - lesson-modules/4-automation/4.5-skills-integration/CLAUDE.md line 11 contains "has already met sub-agents (Module 3.2)"
    - lesson-modules/4-automation/4.5-skills-integration/CLAUDE.md line 11 ends with "Skills complete the trio."
    - lesson-modules/4-automation/4.5-skills-integration/CLAUDE.md contains zero occurrences of "has already learned about slash commands"
    - .claude/commands/start-4-6.md line 2 contains 'description: "Module 4.6: Custom Slash Commands - Explore and customize your vault'"
    - .claude/commands/start-4-6.md contains zero occurrences of "Build /daily"
  </acceptance_criteria>
  <done>Module 4.5 instructor note accurately states students have not yet covered slash commands. start-4-6.md description matches the explore-and-customize teaching philosophy.</done>
</task>

</tasks>

<verification>
After both tasks complete, run a sweep to confirm no stale references remain:

1. `grep -rn "next module (4.5)" lesson-modules/4-automation/` should return zero results
2. `grep -rn "building custom slash commands" lesson-modules/4-automation/4.4-mcp-tools/` should return zero results
3. `grep -rn "already learned about slash commands" lesson-modules/4-automation/4.5-skills-integration/` should return zero results
4. `grep -n "Build /daily" .claude/commands/start-4-6.md` should return zero results
5. `grep -n "/start-4-5" lesson-modules/4-automation/4.4-mcp-tools/CLAUDE.md` should return exactly one result (navigation command preserved)
6. `grep -n "/start-4-4" lesson-modules/4-automation/4.3-librarian-agent/CLAUDE.md` should return exactly one result (navigation command preserved)
</verification>

<success_criteria>
All four stale references corrected:
1. Module 4.4 closing Say describes "Claude Code Skills" (not "custom slash commands")
2. Module 4.3 forward reference says "(4.6)" (not "(4.5)")
3. Module 4.5 instructor note says student has NOT yet covered slash commands
4. start-4-6.md description says "Explore and customize" (not "Build")

Zero stale "(4.5)" references to custom slash commands remain in Level 4 teaching scripts.
Navigation commands (/start-4-4, /start-4-5) are unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/07-renumbering-text-fixes/07-01-SUMMARY.md`
</output>
