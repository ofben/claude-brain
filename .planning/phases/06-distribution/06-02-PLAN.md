---
phase: 06-distribution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DIST-03
  - DIST-04
  - DIST-05

must_haves:
  truths:
    - "A non-technical user reading the README understands what the course will cost them in dollars before they begin"
    - "A reader sees that the course acknowledges BASB, Zettelkasten, LYT, and kepano/obsidian-skills"
    - "A reader finds prerequisites (Obsidian and Claude Code) with links to download/install"
    - "The README module list matches the actual 25 modules in course-structure.json"
    - "The README time estimates match course-structure.json (Level 4: ~155 min, Total: ~8 hours)"
  artifacts:
    - path: "README.md"
      provides: "Polished README with badges, cost estimate, correct module list, acknowledgments, prerequisites"
      contains: "API costs"
  key_links:
    - from: "README.md"
      to: "course-structure.json"
      via: "module count and time estimates"
      pattern: "25 modules"
    - from: "README.md"
      to: "LICENSE"
      via: "license badge"
      pattern: "img.shields.io/badge/license"
    - from: "README.md"
      to: "https://github.com/kepano/obsidian-skills"
      via: "acknowledgment link"
      pattern: "kepano/obsidian-skills"
---

<objective>
Polish the README with badges, cost estimate, updated module list, kepano acknowledgment, and prerequisites links.

Purpose: DIST-03 requires a plain-language API cost estimate. DIST-04 requires kepano/obsidian-skills in acknowledgments. DIST-05 requires prerequisites with links. Additionally, the README's module list and time estimates are stale (missing Module 4.5 "Claude Code Skills" added in Phase 4, Module 4.7 "The Living Vault" rename, and Level 4 time is 157 min not 130 min).

Output: A README.md that accurately represents the current 25-module course with all required distribution information.
</objective>

<execution_context>
@/Users/benest/.claude/get-shit-done/workflows/execute-plan.md
@/Users/benest/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-distribution/06-CONTEXT.md
@.planning/phases/06-distribution/06-RESEARCH.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add badges, cost estimate, and update prerequisites section</name>
  <files>README.md</files>
  <read_first>
    - README.md (current content — must see exact structure before editing)
    - .planning/phases/06-distribution/06-RESEARCH.md (badge syntax, cost estimate text, prerequisites guidance)
    - .planning/phases/06-distribution/06-CONTEXT.md (locked decisions: badges at top, cost estimate in plain language, prerequisites minimal with links, no version numbers)
  </read_first>
  <action>
Edit README.md with these specific changes:

**Change 1: Add badges immediately after the H1 title (line 1), before the intro paragraph.**

Insert after `# Claude Brain: Build Your Second Brain with Claude Code + Obsidian`:

```markdown

![License: MIT + CC BY 4.0](https://img.shields.io/badge/license-MIT%20%2B%20CC%20BY%204.0-blue)
![Modules: 25](https://img.shields.io/badge/modules-25-green)
```

Note: badge says "25" not "24" because Phase 4 added Module 4.5 Claude Code Skills (25 total modules in course-structure.json).

**Change 2: Add API cost callout inside the existing "Requirements" section, after the bullet list and before the Acknowledgments heading.**

Insert after the `- A computer running macOS, Windows, or Linux` bullet:

```markdown

> **API costs:** This course uses Claude's API, which you pay for based on usage.
> Completing all 25 modules typically costs **$1-5 USD** depending on which Claude
> model you choose. Haiku is the most affordable option; Sonnet offers stronger
> responses at a higher cost. You set up billing at
> [console.anthropic.com](https://console.anthropic.com).
```

**Change 3: Rename "Requirements" heading to "Prerequisites"** to satisfy DIST-05's requirement for a prerequisites section. Change `## Requirements` to `## Prerequisites`. The existing content already lists Obsidian (free) with link and Claude Code with link — per CONTEXT.md locked decision, no version numbers needed. The content is already correct; just the heading name changes.

**Change 4: Update the "What You'll Learn" table** to reflect correct times:

Change Level 4 row from:
```
| **4: Automation** | Hooks, agents, MCP tools, custom commands, the living vault | ~130 min |
```
to:
```
| **4: Automation** | Hooks, agents, skills, MCP tools, custom commands, the living vault | ~155 min |
```

Change the total line from:
```
**Total: ~7.5 hours** (work at your own pace — each module is self-contained)
```
to:
```
**Total: ~8 hours** (work at your own pace — each module is self-contained)
```

(492 min = 8.2 hours, rounded to ~8 hours)

**Change 5: Update the Course Structure command list** to include the new Module 4.5 and updated 4.6/4.7:

Replace the command block (lines inside the ``` fence) with:

```
/start-0-1    Install Obsidian
/start-0-2    Install Claude Code
/start-1-1    Welcome to Your Second Brain
/start-1-2    Obsidian Essentials
/start-1-3    Claude Code Essentials
/start-1-4    The Connection (aha!)
/start-1-5    CLAUDE.md — Teaching Claude About Your Vault
/start-1-6    Navigation and Workflow
/start-2-1    Note Types and Templates
/start-2-2    Maps of Content
/start-2-3    Daily Notes and Weekly Reviews
/start-2-4    Research and Literature Notes
/start-2-5    The Knowledge Graph
/start-3-1    Vault-Aware Claude
/start-3-2    Sub-Agents for Knowledge Work
/start-3-3    Claude Reads Your Vault
/start-3-4    Claude Writes for Your Vault
/start-3-5    The Inbox Pattern
/start-4-1    Introduction to Hooks
/start-4-2    Auto-Organize Hook
/start-4-3    The Librarian Agent
/start-4-4    MCP Tools for Your Vault
/start-4-5    Claude Code Skills
/start-4-6    Custom Slash Commands
/start-4-7    The Living Vault (graduation!)
```

This adds the new `/start-4-5 Claude Code Skills` and changes old 4.5/4.6 to 4.6/4.7.
  </action>
  <verify>
    <automated>grep -q "img.shields.io/badge/license" README.md && grep -q "img.shields.io/badge/modules-25" README.md && grep -q "API costs" README.md && grep -q "## Prerequisites" README.md && grep -q "start-4-5    Claude Code Skills" README.md && grep -q "start-4-7    The Living Vault" README.md && grep -q "~155 min" README.md && grep -q "~8 hours" README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `![License: MIT + CC BY 4.0](https://img.shields.io/badge/license-MIT%20%2B%20CC%20BY%204.0-blue)`
    - README.md contains `![Modules: 25](https://img.shields.io/badge/modules-25-green)`
    - README.md contains `> **API costs:**` callout block
    - README.md contains `**$1-5 USD**` in the cost estimate
    - README.md contains `console.anthropic.com` link in cost estimate
    - README.md contains `## Prerequisites` (not `## Requirements`)
    - README.md contains `/start-4-5    Claude Code Skills` in the command list
    - README.md contains `/start-4-7    The Living Vault (graduation!)` in the command list
    - README.md contains `~155 min` for Level 4
    - README.md contains `~8 hours` for Total
    - The command list has exactly 25 entries (0.1-0.2, 1.1-1.6, 2.1-2.5, 3.1-3.5, 4.1-4.7)
  </acceptance_criteria>
  <done>
    README has badges, cost estimate, renamed Prerequisites section, correct 25-module command list, and accurate time estimates matching course-structure.json.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add kepano/obsidian-skills to Acknowledgments section</name>
  <files>README.md</files>
  <read_first>
    - README.md (must re-read after Task 1 edits to see current state)
    - .planning/phases/06-distribution/06-RESEARCH.md (kepano acknowledgment text)
    - .planning/phases/06-distribution/06-CONTEXT.md (locked decision: add kepano/obsidian-skills to existing Acknowledgments)
  </read_first>
  <action>
In the Acknowledgments section of README.md, append a new paragraph after the existing BASB/Zettelkasten/LYT paragraph.

The existing Acknowledgments paragraph ends with: "This course shows what happens when you add an AI partner."

Add a blank line and then this paragraph:

```markdown
This course also makes use of [kepano/obsidian-skills](https://github.com/kepano/obsidian-skills) — an open-source collection of agent skills for Obsidian created by Steph Ango (kepano). The skills taught in Level 4 are drawn directly from this project.
```

Additionally, add a screenshot placeholder. Per CONTEXT.md locked decision, a static screenshot of the Obsidian vault should be added. Since Claude cannot take screenshots, add an HTML image tag with a `docs/vault-screenshot.png` path. Create the `docs/` directory. Place a 1-line placeholder README in docs/ explaining the screenshot is pending.

Insert the screenshot after the "The Story" section's second paragraph (after "...because that's where every second brain starts."):

```markdown

![The Obsidian vault pre-loaded with Alex Rivera's notes](docs/vault-screenshot.png)
```

Then create `docs/` directory and a placeholder note:
```bash
mkdir -p docs
```

Write `docs/SCREENSHOT-NEEDED.md` with content:
```
# Screenshot Needed

Take a screenshot of the Obsidian vault with a few notes open (e.g., the inbox and a project note visible in the sidebar, one note open in the editor). Save as `vault-screenshot.png` in this directory.

This image is referenced by README.md.
```
  </action>
  <verify>
    <automated>grep -q "kepano/obsidian-skills" README.md && grep -q "Steph Ango" README.md && grep -q "vault-screenshot.png" README.md && test -f docs/SCREENSHOT-NEEDED.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - README.md Acknowledgments section contains `[kepano/obsidian-skills](https://github.com/kepano/obsidian-skills)`
    - README.md Acknowledgments section contains "Steph Ango (kepano)"
    - README.md contains `![The Obsidian vault pre-loaded with Alex Rivera's notes](docs/vault-screenshot.png)`
    - `docs/SCREENSHOT-NEEDED.md` exists with instructions for taking the screenshot
    - The kepano paragraph appears AFTER the existing BASB/Zettelkasten/LYT paragraph
  </acceptance_criteria>
  <done>
    Acknowledgments section credits kepano/obsidian-skills alongside existing BASB/Zettelkasten/LYT credits. Screenshot placeholder is in place with instructions for the user to capture it later.
  </done>
</task>

</tasks>

<verification>
- README has license and module-count badges immediately after the H1 title
- README has a plain-language API cost estimate with "$1-5 USD" range
- README has a "Prerequisites" section (not "Requirements") with Obsidian and Claude Code links
- README Acknowledgments credits BASB, Zettelkasten, LYT, AND kepano/obsidian-skills
- README Course Structure lists all 25 modules including 4.5 Claude Code Skills and 4.7 The Living Vault
- README time estimates match course-structure.json (Level 4: ~155 min, Total: ~8 hours)
</verification>

<success_criteria>
- `grep -c "img.shields.io/badge" README.md` returns 2 (license + modules)
- `grep -q "API costs" README.md` exits 0
- `grep -q "## Prerequisites" README.md` exits 0
- `grep -q "kepano/obsidian-skills" README.md` exits 0
- `grep -c "start-4-" README.md` returns 7 (4.1 through 4.7)
- `grep -q "~8 hours" README.md` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/06-distribution/06-02-SUMMARY.md`
</output>
